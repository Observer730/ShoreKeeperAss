<Window x:Class="AIAssistant.Host.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AIAssistant Host" Height="600" Width="900"
        MinHeight="500" MinWidth="760">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" Background="#F5F7FA" BorderBrush="#D8DEE8" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Port" Margin="0,0,8,0" />
                <TextBox x:Name="PortTextBox" Grid.Column="1" Height="30" Text="47621" VerticalContentAlignment="Center" />
                <Button x:Name="StartButton" Grid.Column="2" Width="88" Height="30" Margin="12,0,0,0" Click="StartButton_Click" Content="Start" />
                <Button x:Name="StopButton" Grid.Column="3" Width="88" Height="30" Margin="8,0,0,0" Click="StopButton_Click" Content="Stop" IsEnabled="False" />
                <TextBlock x:Name="StatusTextBlock" Grid.Column="5" VerticalAlignment="Center" Text="Stopped" Foreground="#6B7280" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0,12,0,0" Padding="12" Background="#FAFAFB" BorderBrush="#D8DEE8" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Button x:Name="RecordButton" Grid.Row="0" Grid.Column="0" Width="104" Height="32" Click="RecordButton_Click" Content="Record" />
                <Button x:Name="StopRecordButton" Grid.Row="0" Grid.Column="1" Width="104" Height="32" Margin="8,0,0,0" Click="StopRecordButton_Click" Content="Stop Rec" IsEnabled="False" />
                <ComboBox x:Name="TranscriptionProviderComboBox" Grid.Row="0" Grid.Column="2" Width="128" Height="32" Margin="8,0,0,0" SelectedIndex="0">
                    <ComboBoxItem Content="Local Whisper" />
                    <ComboBoxItem Content="OpenAI" />
                </ComboBox>
                <Button x:Name="TranscribeButton" Grid.Row="0" Grid.Column="3" Width="124" Height="32" Margin="8,0,0,0" Click="TranscribeButton_Click" Content="Transcribe Last" IsEnabled="False" />
                <TextBlock x:Name="RecordingStatusTextBlock" Grid.Row="0" Grid.Column="4" Margin="12,0,0,0" VerticalAlignment="Center" Text="Recorder idle" Foreground="#6B7280" />

                <TextBlock x:Name="RecordingsFolderTextBlock" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Margin="0,8,0,0" TextWrapping="Wrap" />

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Voice:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="VoiceComboBox" Width="200" SelectionChanged="VoiceComboBox_SelectionChanged">
                        <ComboBoxItem Content="danya_xuejie (淡雅学姐 - 清冷)"/>
                        <ComboBoxItem Content="Chinese (Mandarin)_Crisp_Girl (清脆少女)"/>
                        <ComboBoxItem Content="female-yujie (御姐)"/>
                        <ComboBoxItem Content="Chinese (Mandarin)_Gentle_Senior (温柔学姐)"/>
                        <ComboBoxItem Content="Chinese (Mandarin)_Soft_Girl (柔和少女)"/>
                        <ComboBoxItem Content="male-qn-qingse (青涩青年 - 男声)"/>
                        <ComboBoxItem Content="Custom..."/>
                    </ComboBox>
                    <TextBlock Text="Clone voice_id:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                    <TextBox x:Name="CustomVoiceIdTextBox" Width="180" VerticalAlignment="Center" Text="my_voice_001"/>
                    <Button x:Name="CloneVoiceButton" Content="Clone from file..." Margin="6,0,0,0" Padding="8,2" Click="CloneVoiceButton_Click"/>
                    <TextBlock x:Name="VoiceStatusTextBlock" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#666"/>
                </StackPanel>

                <TextBox x:Name="TranscriptTextBox" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="5" Height="70" Margin="0,8,0,0"
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                         Text="Transcript will appear here." />
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Messages">
                <ListBox x:Name="MessagesListBox" BorderThickness="0" />
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Logs">
                <DockPanel>
                    <Button DockPanel.Dock="Top" HorizontalAlignment="Right" Width="80" Height="26" Margin="0,0,0,6" Click="ClearLogsButton_Click" Content="Clear Logs" />
                    <TextBox x:Name="LogsTextBox" BorderThickness="0" IsReadOnly="True" AcceptsReturn="True"
                             TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
                </DockPanel>
            </GroupBox>
        </Grid>

        <DockPanel Grid.Row="3">
            <Button x:Name="SendButton" DockPanel.Dock="Right" Width="96" Height="34" Margin="8,0,0,0" Click="SendButton_Click" Content="Send" IsEnabled="False" />
            <TextBox x:Name="MessageTextBox" Height="34" VerticalContentAlignment="Center" Text="Message from WPF Host" KeyDown="MessageTextBox_KeyDown" />
        </DockPanel>
    </Grid>
</Window>
